/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Fireworks from './Fireworks';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Fireworks);
